%nav#nav
  .navbar
    .navbar-inner
      .container
        = link_to "BPTrack", root_path, :class => "brand"
        %ul.nav
          %li= link_to "Home", root_path
          - if logged_in?
            %li= link_to "MyBP", bloodpressure_readings_path

        %ul.nav.pull-right
          - if logged_in?
            %li= link_to "Account", root_path
            %li= link_to "Sign Out", signout_path
          - else
            %li= link_to "Sign In", "/auth/twitter"